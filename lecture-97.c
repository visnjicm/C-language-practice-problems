#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>

void copyArray(originalArray, int ** copyArray, originalSize)
{
	
}

int main()
{
	int arrOriginal[3] = {1,5,4};
	int * copiedArr;

	copiedArr = (int*) malloc(sizeof(int)*originalSize)

	return 0;
}
